### May 27, 2026
- **Cold Mountain — fixed deploy break from duplicated YAML key, added frontmatter validator** (commit `ef2e355`). Vercel build had been failing with `Failed to collect page data for /wiki/[section]/[slug]` and a stack trace into bundled chunk code — opaque. Reproduced locally: gray-matter died on `~/second-brain/resources/wiki/tools/ai-native-product-development.md` with `duplicated mapping key at line 5, column 1` because the wiki compiler had appended a second `sources: +01kr23gy` line below the existing `sources: +01kqmjaag1`. Fix: removed `sources:` from the vault file entirely (the field doesn't render anywhere — `wiki.ts` only reads `created_at`/`last_updated`). Added a `validateFrontmatter()` pass to `scripts/pull-wiki.mjs` that walks every `.md` under `.wiki-content/` after the copy, parses each with `gray-matter`, and on any error prints `file: message` and exits non-zero — next breakage will be diagnosable from the Vercel log in seconds. Waiting on NanoClaw's 30-min `vault-sync` to push the vault fix to GitHub so Vercel can rebuild; the second build attempt today still failed because the cron tick hadn't fired yet (validator did its job — named the file and line — but the GitHub vault copy was still stale).
- **Surfaced two related issues for the Mini side.** (a) NanoClaw's wiki compiler is the writer that appended the duplicate `sources:` keys — looks like a partial / mis-implemented version of the backlog item *"Add source count to wiki front matter"* (backlog.md line 18), but it's writing Readwise IDs (not counts) and appending instead of merging into a list. (b) `~/second-brain/resources/wiki/tools/voice-ai-infrastructure.md` uses a totally different frontmatter schema (`title`, `created`, `updated`, `tags`) than every other wiki file (which use `created_at`/`last_updated`). The renderer silently ignores the wrong field names, so the article's dates don't display. Both point to schema drift — different agents writing vault frontmatter without checking the existing convention. Drafted three prompts for the Mini covering: (1) urgent — verify the propagated fix and trigger `vault-sync`; (2) investigation — find the wiki-compiler line that writes `sources:`, decide whether to remove it or fix it to a YAML list, then scan the rest of the vault for other duplicate-key frontmatter; (3) cleanup — fix `voice-ai-infrastructure.md` to match the canonical schema.
- **Mac Mini follow-up — root-caused both issues to instruction gaps, not code.** The writer is the per-doc-worker agent inside NanoClaw's `readwise-wiki` group (Claude in a container, dispatched by the list-maker). No instruction file ever told it to write `sources:` in frontmatter — it invented the field on its own and, with no rule, appended a new line each run instead of merging. Same class of invention produced `sources_updated:` in `tools/claude-code-skill-frameworks.md` (latent, hadn't broken yet — single line). The `voice-ai-infrastructure.md` schema mismatch turned out to be an *instruction inconsistency*, not an agent going rogue: `areas/wiki/instructions/readwise-wiki.md` lines 28–34 showed a template with `title`/`created`/`updated`/`tags`, while `per-doc-worker.md` § 4 said `created_at`/`last_updated`. 34 files followed the worker; voice-ai-infrastructure followed the readwise-wiki template literally. Cold Mountain's renderer expects `created_at`/`last_updated`, so the template in `readwise-wiki.md` was the broken one. **Fixes applied** (vault edits, no code changes — pull-wiki.mjs validator remains the safety net): rewrote `readwise-wiki.md` template to canonical schema + added strict "no invented frontmatter keys" rule explaining duplicate keys break Cold Mountain; added the same rule to `per-doc-worker.md` § 4 with an explicit "no `sources:`, `sources_updated:`, `tags:`, `title:`" callout; updated `voice-ai-infrastructure.md` frontmatter to canonical schema (tags dropped — canonical has none); stripped `sources_updated:` from `claude-code-skill-frameworks.md`. Full-vault audit confirms zero remaining duplicate-key files and zero other schema-drift files beyond those two. Worth flagging: backlog.md line 18 (*"Add source count to wiki front matter"*) is now in **direct conflict** with the new strict frontmatter rule — that backlog item should be reconsidered or rewritten before being picked up, since implementing it would require relaxing the rule and rebuilding the renderer to read the field.

### May 26, 2026
- **Readwise wiki — diagnosed and fixed silent per-doc worker failures (root cause: wrong `chat_jid` on cron tasks).** Manifest from 2026-05-24 wrap-up flagged 23 dispatched workers across two weeks with zero commits and no `WORKER_ERRORS.md`. Worker containers were never spawning — list-maker was dispatching to ghosts. Trace: NanoClaw scheduler reads from `~/nanoclaw/store/messages.db`'s `scheduled_tasks` table (a 0-byte `nanoclaw.db` in the repo root is a misleading sibling); `current_tasks.json` is a write-only snapshot for containers via MCP, not the scheduler's input. The `readwise-wiki`, `readwise-wiki-weekly-wrap-up`, and `readwise-wiki-folder-review` cron tasks had `chat_jid = tg:8790860459` (the user's Telegram JID), so their containers inherited `NANOCLAW_CHAT_JID` pointing at telegram_main. When the list-maker MCP-wrapped `schedule_task`, it set `targetJid = chatJid = tg:8790860459`; the host IPC handler's auth check (`!isMain && targetFolder !== sourceGroup`) saw sourceGroup="readwise-wiki" vs targetFolder="telegram_main" and silently dropped the dispatch. NanoClaw is *supposed* to emit `"Unauthorized schedule_task attempt blocked"` in this branch but never did — that's a secondary observability bug in nanoclaw itself worth filing upstream. **Fix:** `UPDATE scheduled_tasks SET chat_jid='task:readwise-wiki' WHERE group_folder='readwise-wiki'` — aligns the tasks' identity with their own group's registered JID (`task:readwise-wiki` from `registered_groups`). Audit confirmed no other groups have the same misconfig: briefing/weekly-summary/monthly-summary/product-briefing tasks correctly point to `tg:8790860459` because they only emit Telegram output and never dispatch sub-tasks. Validated via synthetic IPC payload (hand-crafted JSON dropped to `data/ipc/readwise-wiki/tasks/`) — landed in DB with `"Task created via IPC"` log entry, zero warnings. Then triggered a real list-maker run by editing `next_run` in the DB; container fired, MCP'd `schedule_task` with the corrected chat_jid → DB row created → worker container spawned → `19a824b wiki: update tools/agent-harness.md from 01ksfpy2 2026-05-26` landed on `main` ~22s later. End-to-end pipeline confirmed.
- **Replayed 43 backlog articles via synthetic IPC.** Tried two indirect paths first and both failed: (a) editing `list-maker-log.md`'s `run_start` to `2026-05-10T00:00:00Z` — the list-maker overwrote the cutoff on the next run because the agent reads `run_end` (not `run_start`, despite what the instructions say); (b) editing all three timestamp fields (`run_start`, `run_end`, `updated_after`) to `2026-05-10` — the agent **read** the file but then **substituted** `13:33:00Z` (a recent prior `run_start`) when calling Readwise, apparently judging the `2026-05-10` value as "too old to be real." LLM second-guessing the cutoff is a non-deterministic failure mode for any manual rewind. Switched to direct replay: parsed dispatched doc IDs from `git log -p resources/wiki/list-maker-log.md` since 2026-05-03, dedup'd to 43 unique IDs, generated MCP-shaped IPC payloads (matching the list-maker's exact `schedule_task` prompt template) with `targetJid='task:readwise-wiki'`, staggered `schedule_value` 30s apart, dropped all 43 into `~/nanoclaw/data/ipc/readwise-wiki/tasks/`. NanoClaw consumed and persisted all 43 to the DB within ~5 min. GroupQueue serializes the worker containers (~2-3 min each → ~2 hours total drain). First replay worker fired at `21:39:23Z`, commit landed at `21:39:45Z`.
- **Updated `areas/wiki/instructions/list-maker.md` to encode the cutoff lesson.** Instructions now explicitly say to use the previous run's `run_end` (matching actual agent behavior) and to use the literal value without override "even if it looks unusually old (e.g., a deliberate rewind to catch a backlog)." Future manual rewinds should work.
- **Reverted an earlier mis-fix to `list-maker.md`.** Initially tried `schedule_value="now"` thinking the bug was timezone-related; `new Date("now")` is NaN and the MCP wrapper itself rejects it, plus the actual bug was chat_jid not schedule_value. Reverted to the documented local-ISO format with a note explaining why `"now"` doesn't work.
- **Added intelligence pipeline archival.** `~/nanoclaw/scripts/archive-briefings.conf` previously only covered `daily-briefings:keep=7` and `weekly-products:keep=4` — `weekly-summaries/` (15 files at 2026-W21) and `monthly-summaries/` had no rotation. Added `weekly-summaries:keep=12` (~3 months, mirrors the daily 7-day window one tier up) and `monthly-summaries:keep=all` (only ~12/year, no clutter risk). Next Sunday's `archive-briefings` cron at midnight will move W09/W10/W11 to `weekly-summaries/_archive/` — no restart needed, script re-reads the conf each run.
- **Discovered structural notes worth remembering** (proposing as CLAUDE.md additions separately): NanoClaw stores authoritative task state in `~/nanoclaw/store/messages.db` (not the 0-byte sibling `nanoclaw.db`); `current_tasks.json` is a one-way snapshot, editing it does nothing; restarting NanoClaw is `launchctl kickstart -k gui/$(id -u)/com.nanoclaw`; for any cron task that uses `schedule_task` MCP to dispatch sub-tasks, `chat_jid` must equal the group's own registered JID, not the user's Telegram JID; per-doc worker dispatches that fail authorization are dropped silently by NanoClaw (no warning logged) — symptom is "list-maker runs cleanly, no new wiki commits, no error files" so check for the missing "Task created via IPC" log line if it recurs.


- **Wiki Tutor — paragraph notes + tiered explanations.** Two reader-side additions on top of the click-to-explain panel; shipped in one commit (`61408b6`).

  **A. Paragraph notes (markdown only).** One editable note per paragraph, persisted via a new `paragraph_notes` table keyed on `(user_id, document_id, source_chunk_id) UNIQUE`. Saved notes render in the panel as a clickable card with a pencil affordance (click → switches to textarea); Cmd/Ctrl+Enter saves, Escape reverts an in-progress edit back to display. New gutter mark (pencil) joins the existing eye/sparkle; column order eye → sparkle → note. PDFs excluded — selection IDs are character-offset-based and not re-clickable, so notes would orphan immediately (same reason explanations don't cache on PDFs). Migrations: `0006_paragraph_notes.sql` (table + RLS + `updated_at` trigger), `0007_note_events.sql` (extends engagement events CHECK with `note_save` / `note_delete`).

  **B. Tiered explanations.** The panel now grows through three escalating prompts instead of one. Paragraph click → **TLDR** (the gist) shows. *Still confused?* button → **Expanded** appended below (fills in the implicit bridge: unstated steps, undefined terms, leaned-on assumptions). *Define terms & give an example* → **With examples** appended below (term defs inline + a worked example, drawn from the doc if possible). Prior tiers are passed to the model as `<gist_already_shown>` / `<context_already_shown>` so it knows what NOT to restate — load-bearing, since the design promise is "the tutor adds to what they said, doesn't restart." System prompt also picked up a tutor identity ("you're a tutor sitting next to someone reading"). Each tier caches independently in `paragraph_explanations` (now keyed on `(user, doc, chunk, depth)`); reloads preserve whatever depth the user expanded to. New `explanation_expand` engagement event fires on each escalation — `paragraph_click` can't carry the difficulty signal since every paragraph view starts with one. Migrations: `0008_explanation_depth.sql`, `0009_explanation_expand_event.sql`.

  Brainstorm path before the build was instructive: started with a "version switcher" (three pills swapping between separate explanations), then pivoted when the worry surfaced — "what if Detailed makes me re-read Simple?" — and landed on the additive model where each tier appends below without restating. Label words iterated too: `GIST / CONTEXT / DETAIL` was rejected, a left-bar visual was rejected, then settled on `TLDR / Expanded / With examples` as quiet eyebrow labels above each block. Notes feature also iterated mid-build — first version showed the saved note as an always-on textarea; user wanted it to read as static content with a click-to-edit affordance.

### May 13, 2026
- **Voice Tutor — `/study/` UI redesign + telemetry-inline ended view.** Two big threads, both items from today's daily-to-do (now both `[x]`).

  **A. `/study/` UI redesign via Claude Design handoff** (commit `6bfb4f3`). Ported the Claude Design output for study mode — warm/scholarly aesthetic (paper background `#faf7f0`, Source Serif 4 headings, Inter body, ink-blue accent `#2d4a6b`). New layout: wordmark header, contextual header back link, pulsing listening dot, calm centered typing dots for recap generation, book-style markdown for the recap. UX tweaks during port: upload is one-step (file picker → auto-uploads, no second button click), post-recap CTA reads "Start another session" (stays on the doc rather than dumping the user back to the picker), and the session-idle screen now shows a Haiku-generated 1–2 sentence doc summary so the user knows what they're about to discuss. Summary persistence: written at upload time as `<doc_id>.summary.txt` sidecar; existing docs lazily backfilled in parallel on first `list_documents()` call (~$0.005 total, ~2s one-time hit for the 5 pre-existing docs). Archived two leftover test docs (`Test Document`, `Localhost Smoke Test`) into `~/.voice-tutor/documents/_archive/` to keep the picker scannable.

  **B. Diagnostics panel on the ended view** (commit `d2b7a8f`). One screen now surfaces every artifact a session produces: cost breakdown (tabular monospace, inline), memory append (italic serif, inline + link to full `memory.md`), session analysis (link to viewer), and system prompt (link to viewer). Persistent state in a footer row (memory.md, profile.md, cost-log.md as in-page links). Visual register deliberately distinct from the recap above: tinted background `#f3eee0`, monospace eyebrows, mono small-caps section labels. The recap reads like a printed page; the diagnostics read like a DevTools panel docked beneath. That contrast does the "I measure all of this" demo work.

  Each "View" link opens in the *same* tab (overlay was the original plan; switched to same-tab when the user pointed out that overlays make persistent state feel like a feature rather than the underlying data — different URL = different register). Viewer pages have a quiet "← Back" link that, given `?from=<session_id>`, returns to `/study/?session=<id>` — i.e., the recap-ready view for that specific session, not the picker. Browser back works too. `sessionStorage` preserves scroll position across the round-trip so the user lands where they were.

  **Plumbing additions:**
  - `bot.py` persists the constructed system prompt to `~/.voice-tutor/transcripts/<id>.prompt.txt` at session start so it can be surfaced as an artifact
  - `VOICE_TUTOR_MIN_TELEMETRY_SEC` env var lowers both summary and analysis duration thresholds together (default 120s); set to 60 in `.env` for demos where you don't want a two-minute session — follows the global "make risky changes easy to revert" pattern
  - `GET /api/sessions/{id}/telemetry` composite endpoint returns recap + cost + memory_append + analysis + has_prompt + doc info in one call (single poll, simpler frontend)
  - `GET /api/sessions/latest` for the "View last session →" picker shortcut — useful for both demo (avoid the 20–30s recap-generation latency live) and dev (avoid running fresh voice sessions while iterating on UI)
  - New viewer routes: `/view/memory`, `/view/profile`, `/view/cost-log`, plus per-session `/view/sessions/{id}/prompt` and `/view/sessions/{id}/analysis`

  **Decisions worth remembering:**
  - **Dropped the recap "chrome"** (eyebrow + title + duration meta) above the markdown body. The artifact md already contains its own title and duration, and the duplication caused inconsistent durations between chrome (client-side estimate) and body (server-truth). Lesson: don't wrap a server-generated artifact in client-side scaffolding that duplicates what's already inside.
  - **Diagnostics fill in *alongside* the recap loading state, not before/after.** Original Claude Design treated "Generating recap…" as a full-screen state, hiding everything else behind a 10–30s curtain. Server-side, the recap is actually the *last* artifact to land (summary → memory → analysis → cost → recap), so diagnostics arrive first in practice. Solution: keep the design's clean centered loading visual but place it *inside* the recap-ready view, with diagnostics filling in below. Same visual moment, but the audience watches things materialize one by one instead of staring at typing dots.
  - **Same-tab navigation for viewers, not new-tab.** Original instinct was `target="_blank"` so the session view stayed put. Switched after the user pointed out that overlays/new-tabs feel like product features. The diagnostic register is "you've navigated to the underlying file" — a URL change communicates that more honestly. The viewer pages have their own "← Back" link as the primary exit; sessionStorage holds scroll position across the round-trip.

  **Gotcha discovered (worth a CLAUDE.md note):** `VIEWER_HTML` is a module-level Python string constant in `app.py`. Edits to it require a server restart to take effect — I claimed "no restart needed" twice today and got proven wrong. Static files in `static/` *do* reload per request; Python module state doesn't. Worth documenting in the project CLAUDE.md so I don't keep making this mistake.

### May 12, 2026
- **Voice Tutor — mode parity + demo prep for Beni interview** (continued work after the dev-env migration earlier today). Five themes:
  1. **Pre-connect screen for `/study/`** (commit `2d7c665`). Picking a doc now navigates to a Start/Back screen instead of opening WebRTC immediately. Doc selection is no longer the moment of commitment.
  2. **Mode parity for study mode** (commits `bd9061e`, `f70c221`). Study mode now loads `memory.md` alongside `profile.md` and runs the same post-session pipeline as open chat (summary → memory append → analysis). Reverted the original "doc IS the world" bet — it didn't serve the actual use case (revisiting a paper across sessions) and didn't map cleanly to per-person memory in product analogies. Mitigations for `memory.md` priming the open-chat persona: reframed memory header to "Background — Matt's prior topics (reference only if directly relevant to the document)" and added `STUDY_REMINDER` at the very end of the prompt for recency-priming. `MIN_ANALYSIS_DURATION_SEC` lowered to 120s to match the summary threshold. Also added mic-track release on session end (was leaving the browser mic indicator hot until tab close).
  3. **`/client/` → `/chat/` rename** (commit `4969b36`). `/client/` was a pipecat technical artifact, not a product name. `/chat/` + `/study/` reads as a coherent product.
  4. **README updates**: new "Modes" comparison table (in `bd9061e`) covering persona, prompt context, tools, and post-session work; plus a "Next steps" section (`cb4267c`) capturing tomorrow's plan — polish `/study/` UI (keeping `/chat/` on the prebuilt for its diagnostic value as the architecture-visibility surface), and figure out how to surface telemetry + artifacts inline on the `/study/` ended view (one screen showing cost row + session analysis + memory.md append + recap).
  5. **Demo prep doc updates** in `personal/job-search/`: introduced the two-mode demo strategy (chat first ~60s as architecture proof point, then study as polished product surface) across `demo-script.md` (new Beat 2, total ~7:00), `beni-demo-prep.md`, `demo-format-contingencies.md`, `beni-interview-prep.md` §D, and `architecture-decisions.md` §1.3 (which still claimed study mode strips memory — now describes the new shape, with the failed earlier bet called out explicitly). Archived `demo-fallback-video-script.md` — decided pre-recorded video fallback wasn't a real option.
  
  Decisions worth remembering: kept Voice Tutor's mode naming product-honest rather than Beni-flavored ("I'm building Voice Tutor, not Beni's product — analogies are stronger than mimicry"); decided against building a parallel Vapi POC for the interview (1-2hr docs + hello-world exploration captures 80% of the value, vs days for a real comparison).
- **Voice Tutor — migrated dev environment to MacBook Pro.** Cloned repo, checked out `feat/study-companion-mode`, rsync'd `~/.voice-tutor/` (profile.md, memory.md, transcripts, artifacts, documents — ~700KB) and `.env` from Mac Mini over Tailscale, `uv sync` (103 packages on Python 3.12.13), confirmed `./start.sh` listens on :7860 and `/study/` loads. Personal data flow: `~/.voice-tutor/` is rsync'd manually; vault-side `cost-log.md` + `session-analyses/` arrive via the hourly vault sync cron. Tailscale only needed for the cross-machine copy — local dev hits localhost.
- **Voice Tutor — fixed "Generating recap…" hang on short/failed sessions.** Ending a session before any conversation happens never produces a transcript, so `generate_artifact` is never fired (bot.py:611 returns early on empty `turns`). The UI was polling `/api/sessions/{id}/artifact` indefinitely and showing "Generating recap…" forever. Two-part fix in `static/study.html` (commit `6621291`, pushed): (1) skip the recap view entirely when the session lasted <5s after reaching `'connected'` — show "Session ended — too short for a recap"; (2) cap polling at 30 attempts (60s) and surface "Recap didn't generate within 60s — check server logs" if no artifact arrives. Best-practices framing: client should never poll without an upper bound, and shouldn't show "working on it" for work that was never started. The two checks handle different failure modes (empty session vs server-side generation failure).
- **Migration gotcha to remember**: editing repo files on Mac Mini while the running server lives on the MacBook Pro means changes don't propagate. Caught this when `MIN_RECAP_DURATION_SEC` wasn't in the MBP's `study.html` after a hard refresh. Fix: commit on the editing machine, `git pull` on the other (or rsync the one file for a quick test).

### May 11, 2026
- **WikiTutor — explanation cache made persistent + reader UX polish + dwell-gated reads + brevity prompt + strategic recheck.** Two commits: `1890752 feat: persistent explanation cache + reader gutter layout + font bump`, `24a71e8 feat: reader UX polish (active bar gutter, panel-aware chrome, dwell-gated reads, brevity prompt)`. Branch is 2 ahead of origin (NOT pushed yet). **(1) Persistent explanation cache** — sparkles were a persistent signal (hydrated from `engagement_events`) but the explanation cache was in-memory only, so on reload every sparkle-bearing paragraph re-paid the Claude call. Migration `0005_paragraph_explanations.sql` adds a per-(user, document, source_chunk_id) UNIQUE table; `/api/explain` upserts on success when `source = 'markdown_upload'` (PDF selection ids are character-offset based and never re-clickable, so PDF rows would never be read); `/document/[id]/page.tsx` queries the table in parallel with engagement events (`Promise.all`) and passes `initialExplanations: Record<string, string>` to `MarkdownReader`, which seeds `cache.current` Map on mount. First explanation wins (`onConflict: ignoreDuplicates`); to invalidate after a prompt change, `DELETE FROM paragraph_explanations` in the SQL editor — no in-app regen. Bulk pre-load at SSR vs lazy fetch-on-click decision: bulk for now (~2KB × 30 explanations = 60KB extra payload, negligible vs document text); switch to lazy when a single doc routinely crosses ~200 explanations. **(2) Don't-abort-on-supersede.** Earlier behavior: clicking paragraph A → click B mid-flight → A's `AbortController.abort()` killed the network request → A never reached the cache, so revisiting A meant another Claude call. New behavior: drop `signal: ctrl.signal` from the fetch; let the request complete; `cache.current.set()` always fires on success; `if (ctrl.signal.aborted) return;` only suppresses UI updates. The AbortController is now a "is-this-still-visible" flag, not a network kill-switch. Both readers (markdown + PDF) updated for consistency though only markdown benefits — PDF cache keys are non-stable. **(3) Reading-progress marks moved from inline-end to a 32px left gutter.** Inline-at-end-of-last-line forced eye-scanning the jagged right edge to find marks; left gutter aligns marks on a single x-coordinate down the article. `padding-left: 32px` on every block-level prose element (`p`, `h1`/`h2`/`h3`, `ul`/`ol`, `blockquote`) carves the gutter; `.p-marks` is `position: absolute; left: 0; top: calc(4px + 0.5lh - 5px); flex-direction: column;`. First eye icon vertically centered on the paragraph's first line via the `lh` unit. Hidden below 768px viewport (mobile uses macro progress bar; no horizontal real estate for a gutter). **(4) Active-paragraph highlight clipped to text only** via `background-clip: content-box` so the gutter icons aren't painted over by the `--hl-soft` fill. Active accent bar moved from `box-shadow: inset 3px 0 0 var(--accent)` to a `::before` pseudo at `left: 24px` (in the gutter, 5px gap to text). **(5) Two CSS gotchas hit + captured in project CLAUDE.md.** (a) **`background:` shorthand resets `background-clip`** — when I added `background-clip: content-box` to the base `<p[data-pid]>` rule but kept `background: var(--hl-soft)` on `.is-active`, the shorthand silently restored `background-clip: border-box` and the highlight flooded the gutter again. Fix: use `background-color:` longhand on hover/active. (b) **`lh` unit on element with `font-size: 0` resolves to 0** — initially had `font-size: 0` on `.p-marks` (left over from when it was inline-flex and needed whitespace suppression), which made `0.5lh = 0`, putting the marks at `top: -3px` (above the paragraph). Fix: removed `font-size: 0`; the icons are 14×14 SVGs with explicit dimensions, font-size irrelevant. **(6) Article font 18 → 20px.** Long-form readability — Substack/Medium territory. **(7) Progress bar + Resume button now panel-aware.** Earlier: progress bar (`position: fixed; top: 47px`) and Resume button (`position: fixed; right: 20px`) ignored body's `padding-right` shift, so the explain panel covered them. Now: progress bar uses `transform: scaleX(progress)` on an element spanning `left: 0; right: 0`, with `body.explain-open .reading-progress { right: min(420px, 92vw); }` clipping the track to the visible reader area; Resume button gets `right: calc(20px + min(420px, 92vw))` shift in lockstep with the panel slide (same 280ms cubic-bezier). Mobile uses bottom-sheet panel — both stay at default position. **(8) Active bar padding** — `.is-active::before` shifted from `left: 32px` (text edge, no gap) to `left: 24px` so the bar sits 5px inside the gutter, visibly separated from the highlighted text. **(9) Dwell-time threshold for `paragraph_read`.** Earlier: `IntersectionObserver` fired immediately on entry into the upper 25% — fast scrolling marked everything as read. New: enter zone → `setTimeout(1500)` scheduled; leave zone before timeout → `clearTimeout`; timeout fires → log `paragraph_read` + add to `readIds`. Pending timers cleared on unmount. 1.5s arbitrary but tuneable; even at slow scroll any paragraph stays in the upper-25% zone for ~2-5s, fast scroll <1s. **(10) Brevity prompt rule for `/api/explain`.** Old: "Keep it to 2–4 sentences unless the passage genuinely requires more." Produced explanations longer than the source on short passages — defeats the point. New: "Aim for an explanation shorter than the passage itself. If the passage is one sentence, give a one-sentence gist. Go longer only if the passage genuinely needs unpacking." Cached explanations from the old prompt aren't auto-invalidated — manual `DELETE FROM paragraph_explanations` to refresh. **(11) Pushback on two proposed features kept the surface calm.** User asked about a regen button on the explain panel and about clickable eye icons for manual unread/read marking. Pushed back on both — regen because feature #7 (chat / follow-up Q&A) is the next planned feature and subsumes regen as one possible follow-up, manual eye marking because `engagement_events` is intentionally append-only (migration 0002, load-bearing for the recall architecture) and the use case was speculative. Settled on dwell-time threshold (#9) as the simpler fix for the actual complaint. **(12) Strategic recheck mid-session: "I feel like I'm building Readwise."** Honest read — yes, the reading layer (auth, upload, library, reader, click-to-explain, read-tracking) is connective tissue any reading app needs, including Readwise Reader. The validated 2026-04-26 pain was specifically "no listener for active recall," not "I want a nicer reader." Differentiator lives in the unbuilt #7→#9 loop (chat → session → artifact). **Recommendation for next session**: stop polishing the reading view; ship a crappy artifact prototype (#9 with mocks) over today's engagement events to validate whether the recap concept itself feels valuable, before investing in chat + session infrastructure. If the recap lands flat, building chat to feed a flat artifact is wasted scope. Vault docs updated: `~/second-brain/products/wiki-tutor/README.md` Status section refreshed + new "Strategic flag (2026-05-11)" section. (2026-05-11)

### May 7, 2026
- **WikiTutor web — added document delete + reading-progress feature (eye/sparkle gutter icons, in-reader progress bar, Resume button, library row progress bars). Uncovered and fixed a load-bearing `dangerouslySetInnerHTML` memoization gotcha mid-session.** No commits yet — work staged across `~/Development/wiki-tutor/`. **(1) Document delete.** New `DELETE /api/documents/[id]` handler — auth → fetch row (RLS scopes ownership) → `removeKeys` for `original_file_path` + `markdown_file_path` → `documents.delete()`. RLS policies on documents and storage.objects already in place from migration 0001; `engagement_events.document_id` cascades. Library row UI: hover-revealed trash icon (red on hover) replaces former delete text button; click → inline two-step confirm (`cancel · confirm delete` with focus jumping to confirm), Esc / outside-click / 4s auto-revert dismisses; on confirm, row dims, fades + collapses (200ms transition on opacity + max-height) before `router.refresh()`. Replaced `window.confirm` to fit the calm aesthetic. **(2) Reading-progress feature** — gutter marks per paragraph in markdown reader. Two events tracked: `paragraph_read` (new) fires when a paragraph crosses into the upper 25% of the viewport (`IntersectionObserver` with `rootMargin: "0px 0px -75% 0px"`, threshold 0.01, fired once per paragraph per session via a `Set<string>` seeded from `initialReadIds`); `paragraph_click` (existing) drives the sparkle. Migration `0003_paragraph_read_event.sql` adds `paragraph_read` to the `engagement_events_type_chk` CHECK; `EngagementEventType` in `src/lib/engagement.ts` and `ALLOWED_EVENT_TYPES` in `src/app/api/engagement/route.ts` updated in lockstep (per CLAUDE.md "all three or none"). `/document/[id]/page.tsx` queries `engagement_events` for prior `paragraph_read` + `paragraph_click` rows and seeds `MarkdownReader` with `initialReadIds` / `initialExplainedIds` so marks survive reloads. Marks render via imperative DOM injection inside each `<p data-pid>`: a single `<span class="p-marks">` container with eye + sparkle children, reconciled by a no-deps `useEffect` that walks `[data-pid]` blocks every render. **Eye-before-sparkle DOM order** (when both present) — `mark.prepend(makeMark("eye"))` instead of append. **(3) Critical bug + fix: `dangerouslySetInnerHTML` re-applies innerHTML when the prop is a fresh `{__html: html}` object literal each render, wiping all imperatively-injected children — even when the html STRING is identical.** Symptom: `[reconcile-debug] beforeMarks: 0` on every render despite earlier reconciles having added eye spans. React's prop diff for `dangerouslySetInnerHTML` short-circuits when `lastProp === nextProp` (object identity), but with an inline object literal that's never true; the secondary string-equality check should prevent re-application but in practice the spans were getting wiped. Fix: `useMemo(() => ({ __html: html }), [html])` to keep the prop reference stable. After memoization, `beforeMarks` grew across renders (9 → 10 → 11) and `eyesAlready` reflected accumulated state. Also memoized the `style={{ fontSize: 18, lineHeight: 1.72 }}` prop defensively. **This is load-bearing for any imperative DOM injection inside `dangerouslySetInnerHTML` containers.** **(4) Robust DOM-children lookup.** Replaced `p.querySelector(":scope > .p-marks")` with manual `Array.from(p.children).find(...)` walks for both the marks container and inner mark children. `:scope >` was either misbehaving in the user's Chrome or just coincidentally fine — manual walk is universally portable. **(5) Iterative position polish.** Tried five icon positions across the session before landing: absolute right gutter → bottom-right (looked attached to next paragraph) → left vertical gutter (clipped/wiped) → inline at end of paragraph (rejected) → final: inline-flex at end of paragraph with `margin-left: 8px; vertical-align: baseline`. Same on mobile and desktop. Eye fade-in has `animation-delay: 0.5s` (sparkle has none — appears immediately on click). Three iterations on the sparkle SVG before settling on three-star composition (one main concave 4-point + two smaller stars). Opacity 0.4 (low but visible), no hover boost. **(6) In-reader progress bar.** 2px accent-colored bar pinned at `top: 47px` (overlaying the ReaderShell header's existing 1px bottom border, height 48px = h-12). `width: ${progress * 100}%` with `transition: width 0.4s ease`. Visible from page-load (reflects prior-session reads). Track is implicit — the header's existing border shows through the unread portion. **(7) Resume button** in the header (top-right, fixed, mirrors typographic style of `← Library`). Click → `lastReadPid` (highest-index paragraph in `readIds` per document order via `useMemo`) → `target.scrollIntoView({ behavior: "smooth", block: "start" })`. Disabled at 35% opacity / not-allowed cursor when no paragraphs have been read yet. **(8) Library-page progress bars per row.** Migration `0004_paragraph_count.sql` adds nullable `paragraph_count integer` column to documents. Upload pipeline (`/api/documents/route.ts`) computes `renderDocumentMarkdown(extracted.markdown, "preview").paragraphs.length` for new markdown uploads and writes it. Lazy backfill in `/document/[id]/page.tsx`: when reader page renders and `doc.paragraph_count == null`, write `rendered.paragraphs.length` so legacy docs fill in on next open. Library page (`src/app/page.tsx`) queries `engagement_events` for `paragraph_read` rows, builds `Set<source_chunk_id>` per `document_id`, passes `paragraph_count` + `read_count` to `<Library>`. Each row renders a track (full row width, `background: var(--rule)`) with an accent-colored fill (`width: ${progress * 100}%`) between title and meta date in flow. Track only renders when `paragraph_count` is non-null; PDFs and not-yet-opened legacy docs show no bar. Removed top divider lines (`borderTop` on row container) — progress bar provides enough visual structure between rows. **(9) Diagnostic noise gone.** Temporary `[read-debug]` and `[reconcile-debug]` console logs added during the bug hunt have all been stripped — final tree is clean. **Files touched**: 9 modified, 2 new (both migrations). Landed as three logical commits on `main` — `3c91c3b feat: document delete (UI + endpoint)`, `83b6358 feat: reading-progress feature (eye + sparkle marks, in-reader progress bar, Resume)`, `3f74397 feat: per-document reading progress (paragraph_count + library row bars)`. To split single-file diffs (Library.tsx, globals.css, document/[id]/page.tsx) across commits, used a temp-file revert/restore dance: `cp` final state to `/tmp`, edit-out the later-commit-only sections, commit, then `cp` back. Tedious but produces clean per-feature commits. **Repo published to private GitHub `mattli/wiki-tutor`.** No prior remote — created via `gh repo create wiki-tutor --private --source=. --remote=origin`, pushed `main` clean. All commits already on `mattli@users.noreply.github.com` (set repo-local at some prior point), so GH007 didn't fire — unlike Voice Tutor on May 6 which needed filter-branch rewrite. User applied both migrations during the session via the Supabase SQL editor. (2026-05-07)

### May 6, 2026
- **Voice Tutor — published to private GitHub repo `mattli/voice-tutor`.** No remote existed before; created via `gh repo create voice-tutor --private`, both `main` and `feat/study-companion-mode` pushed. First push rejected by GH007 (commit emails were `mattli8020@gmail.com`); rewrote all 24 commits across both branches to `mattli@users.noreply.github.com` via `git filter-branch --env-filter` (matches MacBook Pro git config), set repo-local `user.email` to noreply, re-pushed clean. Pre-push secret scan was clean — `.env` is gitignored, README hits on `ANTHROPIC_API_KEY=` etc. were placeholder docs not real keys. Added a "Local development setup" section to the repo README (clone → `uv sync` → `.env` → `~/.voice-tutor/` → wiki → run; Tailscale moved to a sub-section since it's deployment-specific) so future-machine setup doesn't require digging through prior chat. Committed as `b0e5b2d` on `feat/study-companion-mode`, pushed. Mac Mini stays primary; the MacBook Pro clone path is documented but not yet executed (deferred). (2026-05-06)

### May 1, 2026
- **WikiTutor web — built PDF native selection + engagement metadata layer, then validated extraction-based reading as the architectural successor to native PDF.** Six commits on `main` in `~/Development/wiki-tutor/`. **(1) `148d652` — PDF native browser text selection + `engagement_events` table.** Replaced disabled Option B sentence-overlay code in `PdfReader.tsx` with a mouseup-driven native-selection flow: `<PdfReader>` wraps pages in `.pdf-selection-host`, on `mouseup` reads `window.getSelection()`, validates origin in container + `≥3` chars after whitespace-collapse + start node inside a `[data-page-number]` element, computes `selectionId` as `<docId>-page<N>-off<charOffset>` via `Range.toString().length` from `.textLayer` start to selection start, anchors a floating `<SelectionMenu>` ("Explain" pill with `mousedown.preventDefault` to preserve native selection across click) above the first client rect, and on Explain calls the existing `/api/explain` route (also extended to accept PDFs — reads `markdown_file_path` for `pdf_upload`, `original_file_path` for `markdown_upload`). Same `<ExplainPanel>` for both surfaces; only the trigger differs. Edge-case decisions captured in `CLAUDE.md`: cross-page selections anchor on first client rect; `>4000` char selections truncated; `selectionchange → collapsed` dismisses; native selection clears on first click outside (we don't paint a custom persistent overlay — that's the resurrection of the system we just deleted). **Engagement layer** (migration `0002_engagement_events.sql`): `id, user_id, document_id, session_id, event_type, source_chunk_id, event_metadata jsonb, created_at`; RLS-scoped per user; CHECK constraint on `event_type` enum (`paragraph_click`, `pdf_selection_start`, `pdf_selection_explain`, `panel_dismiss`, `follow_up_action`); append-only (no update/delete policies). New `src/lib/engagement.ts` (fire-and-forget client helper with `keepalive: true`) + `src/app/api/engagement/route.ts` (auth + ownership check + insert). `sessionId` generated per page-load via `crypto.randomUUID()`. **No v1 feature consumes this** — captured because retrofitting the engagement layer is impossible after the fact (per `~/second-brain/products/wiki-tutor/recall-architecture.md` § "v1 implications"). MarkdownReader instrumented with `paragraph_click` + `panel_dismiss`; PdfReader with `pdf_selection_start` + `pdf_selection_explain` + `panel_dismiss`. **(2) `ab91d70`, `7d7be05`, `a54c3be` — three rounds of debugging visible PDF selection jankiness.** First round: `::selection { color: var(--ink-strong) }` was un-hiding react-pdf's normally-`color: transparent` textLayer glyphs, painting a duplicate of the canvas-rendered text on top during selection. Dropped the `color` rule, pinned `.textLayer` + spans to `color: transparent` defensively. Second round: `setActive` updates were re-rendering `PdfReader` on every mouseup, walking react-pdf's internal Page reconciliation; wrapped `PdfPageFrame` in `React.memo`. Bumped `::selection` to `--hl-active` (36% accent) for decisiveness. Third round: drag-time canvas repaints when crossing line breaks — added `contain: layout paint` + `transform: translateZ(0)` to `.pdf-page-frame` and `will-change: transform` to `.textLayer` so the compositor can isolate paint layers. **(3) `d0ae114` — froze further investment in PDF native selection.** After the third round, user surfaced an architectural realignment: the jitter, paragraph-snapping over absolutely-positioned spans, and overlay artifacts are symptoms of a deeper PDF-as-reading-surface mismatch, not bugs in our code. Resolution direction: extract PDFs to high-quality markdown at upload, default reading view to markdown, original PDF as "view source." Added a freeze note at the top of CLAUDE.md's PDF section ("Do NOT attempt to patch these"); saved a feedback auto-memory (`feedback_no_pdf_selection_patches.md`) so future-me doesn't reflexively try to patch native selection again. **(4) `951051d` — fixed engagement-event double-logging in dev.** Diagnostic SQL on dogfood data showed 87 rows after a short session, every `pdf_selection_start` firing exactly twice ~10–20 ms apart with identical `source_chunk_id`. Fingerprint = React 18 StrictMode dev double-invocation. Root cause: `logEngagement` was sitting inside a `setActive((prev) => { ... })` updater function — StrictMode intentionally invokes updater functions twice in dev to surface impurity. Fix: hoist the call into the event handler directly, dedupe rapid repeats via a `useRef` (refs aren't replayed by StrictMode), reset the ref on `selectionchange → collapsed`. Audit confirmed the other event types (`paragraph_click`, `panel_dismiss`, `pdf_selection_explain`) were already correct (logged from handlers with non-functional setters). Added a load-bearing CLAUDE.md note: "Events fire ONCE per user action — log from event handlers, NEVER from `useEffect`s responding to state OR from inside `setX((prev) => ...)`. Dedupe via `useRef`, never via state." User truncated `engagement_events`, re-tested, confirmed counts match real actions. **(5) Extraction quality experiment (no commits — research output in `/tmp/extraction-test/`, fixture copied to `test-fixtures/marker-twitter-article.md`).** Per the architectural realignment, tested whether PDF→markdown extraction is good enough to make markdown-default reading the v1 PDF flow. Generated test PDF from `~/second-brain/resources/reads/2026-04-30-what-to-learn-build-skip-ai-agents.md` via headless Chrome print-to-PDF. **PyMuPDF4LLM** (737ms): preserved h1/h2 + bold/italic + text fidelity, BUT every PDF visual line became its own paragraph — 371 click-units vs source's 100, median paragraph length 92c vs 285c, max length 102c vs 791c, 13% fragments under 40c vs 2%. Verdict: not good enough — click-units correspond to PDF line widths, not ideas. **Marker** (47.9s on first run after 38s install — required `brew install python@3.11` venv since marker-pdf 1.10.2's `surya` dep needs Python 3.10+ syntax which 3.9.6 cannot parse): 103 click-units (vs 100), median 282c (vs 285c), max 791c (IDENTICAL), 2% fragments (IDENTICAL). Hits every criterion within the noise of the source itself. Heading-level offsets (h2 → h1, h3 → h4 in places) are normalizable with a regex pass at upload time; not blocking. **Verdict: extraction-based reading is architecturally viable with Marker-quality extraction.** Architectural decision still belongs to the user — open question is whether to commit to extract-and-render pipeline (build cost: async extraction job queue since Marker is too slow for sync uploads, heading normalization pass), test API extractors (Reducto/Llamaparse) for speed/quality, or live with native PDF jank as v1 reality. Test fixture staged at `test-fixtures/marker-twitter-article.md` (gitignored) for hands-on dogfooding. **(6) `/ce:compound` documentation pass.** Captured the StrictMode double-fire as the first entry in `docs/solutions/`: `docs/solutions/logic-errors/pdf-selection-start-double-logged-strictmode-2026-05-01.md` — bug-track doc with before/after code, a CI grep guard, an ESLint `no-restricted-syntax` rule, Playwright + Vitest regression test patterns, code-review checklist, and diagnostic SQL window function. Discoverability check added a one-liner pointer to `docs/solutions/` at the top of CLAUDE.md so future agents (this plugin or otherwise) discover the knowledge store. **Files touched across the session**: new `supabase/migrations/0002_engagement_events.sql`, `src/app/api/engagement/route.ts`, `src/lib/engagement.ts`, `src/components/reader/SelectionMenu.tsx`, `docs/solutions/logic-errors/pdf-selection-start-double-logged-strictmode-2026-05-01.md`, `test-fixtures/marker-twitter-article.md` (gitignored); modified `src/components/reader/PdfReader.tsx` (replaced overlay machinery + native selection + memoization), `src/components/reader/MarkdownReader.tsx` (engagement logging), `src/app/api/explain/route.ts` (allow PDFs), `src/app/document/[id]/page.tsx` (drop pageSentences prop), `src/app/globals.css` (selection menu + ::selection accent + textLayer transparent + compositor hints), `CLAUDE.md` (hybrid trigger architecture + engagement metadata + logging discipline + PDF freeze note + docs/solutions/ pointer), `.gitignore` (added `/test-fixtures/`). Repo head clean; no uncommitted changes at session end (all six commits already on `main`, not pushed). (2026-05-01)
- **NanoClaw — humanized streaming progress lines for the Telegram main agent.** Built on top of yesterday's `NANOCLAW_STREAMING_PROGRESS=1` flag. Added a `formatToolUse(name, input)` helper in `container/agent-runner/src/index.ts` that maps SDK tool_use blocks into per-tool human-readable lines instead of bare `🔧 <ToolName>`. Map covers `Read → 📖 Reading <file_path>`, `Edit/NotebookEdit → ✏️ Editing <file_path>`, `Write → 📝 Writing <file_path>`, `Grep → 🔍 Searching for "<pattern>"`, `Glob → 🔍 Finding <pattern>`, `Bash → ⚙️ Running: <command>`, `WebFetch → 🌐 Fetching <url>`, `WebSearch → 🔎 Web search: <query>`, `Task/Agent → 🤖 Spawning agent: <description>`, `TodoWrite → 📝 Updating todos`, `Skill → 🎯 Skill: <skill>`. MCP tools (`mcp__<server>__<tool>`) render as `🔌 <Server>: <tool>`. Unknown tools fall back to `🔧 <name>` (today's behavior preserved). All formatting lives in the container — host `processGroupMessages` just appends `result` to the buffer, so no schema change to `ContainerOutput`. Inputs are truncated to ~80 chars (60 for queries/agent descriptions) and string values are JSON-stringified for safety. Decided NOT to add a second feature flag for this — the existing `NANOCLAW_STREAMING_PROGRESS` flag is enough of a kill-switch and per-tool formatting is cheap. Stale `data/sessions/{readwise-wiki,briefing,telegram_main,monthly-summary}/agent-runner-src/` copies archived to `data/sessions/_archive/agent-runner-src-<timestamp>/` (per global "never delete files" rule; auto-regenerated on next spawn anyway). `npm run build` clean, NanoClaw restarted via `launchctl kickstart -k gui/$(id -u)/com.nanoclaw`, Telegram bots reconnected. Committed as `f7c2f73` on `main` (1 ahead of origin/main, not pushed yet). Bundled in same commit: pure prettier reformatting in `src/index.ts` and `src/channels/telegram.ts` that was sitting uncommitted from yesterday's streaming-progress work.
- **NanoClaw — fixed `FOLDER_REVIEW.md` → `folder-review.md` in scheduled task prompt and IPC mirrors.** The `readwise-wiki-folder-review` scheduled task (cron `0 3 1 * *`, monthly) had a prompt referencing `FOLDER_REVIEW.md` (uppercase) in three places, but the actual vault file at `~/second-brain/resources/wiki/folder-review.md` and all instructions docs are kebab-case. Updated `store/messages.db` `scheduled_tasks.prompt` for that task id (3 occurrences), then synced the IPC mirrors at `data/ipc/readwise-wiki/current_tasks.json` and `data/ipc/telegram_main/current_tasks.json` (Python in-place rewrite). Left untouched: historical entries in `session-tasks.md` (literally describes the original rename), `scheduled_tasks.last_result` text from past runs, `task_run_logs.result` rows, `.jsonl` SDK transcripts, and `messages.db.backup-2026-04-27-per-doc-pipeline` — all are records of past activity, not instructions. Next monthly run will read the corrected prompt. (2026-05-01)

