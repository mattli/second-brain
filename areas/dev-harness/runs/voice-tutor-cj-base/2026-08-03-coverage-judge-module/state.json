{
  "runId": "msdsrc8x",
  "goal": "# Goal: Coverage Judge Module (standalone, hermetic + label-verified)\n\n**Date:** 2026-08-03\n**Target repo:** voice-tutor (`~/development/voice-tutor`)\n**Type:** self-contained new module — harness lane (no live-code changes, no app wiring)\n**Design doc:** `~/second-brain/products/voice-tutor/planning/2026-08-02-live-coverage-design.md`\n**Eval set:** `~/second-brain/products/voice-tutor/validation/coverage-experiment/labels.json`\n\n## What to build\n\nA standalone Python module `coverage_judge.py` (plus tests) that judges claim coverage for one study session:\n\n- **Input:** a claim list (id + claim text), an indexed transcript (list of `{index, role, content}` turns), and config (model, judge-prompt version).\n- **Output:** a verdict object — for every claim id exactly once: `{claim_id, covered: bool, turns: [int, ...]}` — plus metadata: `judged_at`, `model`, `judge_prompt_hash`.\n- One model call per invocation (Haiku, temperature 0), strict-JSON response parsing with the transport-layer defenses this repo has learned the hard way: markdown-fence stripping, truncation detection (verdict count must equal claim count), and a bounded retry on malformed output.\n- A pure function `union_coverage(verdict_sets) -> {covered_ids, percentage}` that merges verdicts across sessions per the design doc (union by claim id, percentage derived at read time).\n- A small CLI: `python -m coverage_judge --claims <file> --transcript <file> --out <file>` so the module is runnable standalone before any app wiring exists.\n\n## The judge prompt is part of the deliverable — v2, not v1\n\nStart from `judge-prompt-v1.md` in the eval-set folder (hash `632b73a34b1a22b1`). v1 is strict against topic adjacency but has ONE PROVEN FAILURE: it credits *fluent wrongness* — an explanation that matches a claim's shape and keywords while contradicting its specific content (see the c30 note in labels.json: the tutor taught the doc's sub-example triad as the headline three-way split, and v1 credited it).\n\n**v2 must add a content-match requirement:** covered means the tutor's explanation is *consistent with the claim's specific assertions* — same substance, not merely same shape, topic, or shared keywords. An explanation that substitutes different specifics (different list members, different mechanism, contradicting details) is NOT covered even if fluent and adjacent.\n\nRecord the v2 prompt in the module (versioned, hashed the same way) so every verdict carries `judge_prompt_hash`.\n\n## Verification (contract acceptance criteria)\n\nTwo layers, per this repo's learned rules:\n\n**1. Hermetic tests (no network):** input validation, JSON parsing defenses (fenced output, truncated verdict lists, missing claim ids), union logic, CLI wiring, deterministic prompt-hash computation. Mock the model call.\n\n**2. Credentialed smoke against the eval set (REQUIRED — this is the acceptance bar, not an afterthought):**\nRun the real judge (real Haiku call, key from the repo `.env` — never echo it) against the frozen fixtures and require **label agreement**:\n\n- Fixtures: the four PRIMARY transcripts + the matt doc's 63-claim list, and the strictness-test transcript + the shared doc's 71-claim list. Copy them into the module's test fixtures from:\n  - transcripts: `~/.voice-tutor/transcripts/matt/` (sessions f6148c26, 7beee170, d33800bf, bb979045, 12f3a30d)\n  - claim maps: the two docs' `.claims.json` sidecars\n- **Pass criteria against `labels.json`:**\n  - The 16 upheld claims (c1,c2,c3,c5,c6,c8,c9,c15,c17,c21,c27,c28,c31,c46,c47,c48) must come back covered in the union of the four primary sessions.\n  - **c30 must come back NOT covered** — this is the mandatory regression case; v1 fails it, v2 must not.\n  - Strictness test: session 12f3a30d against the 71-claim map must produce ≤ 2 covered claims (v1 produced exactly 1; small headroom allowed, but crediting a handful means the strictness regressed).\n- If v2 prompt changes flip any of the 16 upheld labels to not-covered, that is a FAILURE of this run's criteria (do not silently trade recall for the c30 fix). Iterate the prompt until all 17 labels agree.\n\n## Constraints\n\n- New files only. Do not modify `bot.py`, the server, prompts, or any live path. App wiring is a separate, later CC-session task.\n- Module lives in the repo (suggested: `coverage/` package or alongside existing standalone tools — follow the repo's existing module conventions).\n- Costs: each credentialed smoke run is ~5 Haiku calls (~$0.07). Bound smoke invocations sensibly (don't re-run per hermetic test).\n- Respect the ledger pattern if trivially available, but do NOT wire ledger writes into the app — a `--cost-out` JSON from the CLI is sufficient for now.\n- Runs on Matt's Claude subscription — normal harness usage limits apply.\n\n## Out of scope (explicitly)\n\n- The live in-session judge loop, sidecar writes into `~/.voice-tutor`, UI/polling, topics rollup, teardown integration — all later CC work per the design doc.\n- Any change to claim extraction or the claim maps.\n- Re-judging or modifying `labels.json` — it is the frozen answer key. If the generated judge repeatedly fails on one specific label and the disagreement looks principled, STOP and surface it in the run report rather than tuning the prompt to force agreement.\n\n## Success looks like\n\n`coverage_judge.py` + tests merged on a branch, hermetic suite green, credentialed smoke green with 17/17 label agreement (16 covered + c30 not-covered) and strictness ≤ 2 on the trap session, v2 prompt versioned and hashed, CLI runnable end to end. Ready for a CC session to wire into the app.\n\n---\n\n## Launch 2 amendments (2026-08-03)\n\n*Additive amendments for the second launch, after Launch 1 (run `msdql2bo`) paused on the per-sprint wall clock partway through Stage 1. The record above is unchanged; these amendments override it where they conflict.*\n\n### The v1 prompt is INPUT, not a deliverable — verify, never reproduce\n`judge-prompt-v1.md` is a fixed input to VERIFY, not something to author or reconstruct.\n- Copy `judge-prompt-v1.md` from the eval-set folder into the module's test fixtures **verbatim** (byte-for-byte).\n- The hash `632b73a34b1a22b1` is **provenance to verify on the copied file**: hash the copied bytes with the module's hash function and assert it equals `632b73a34b1a22b1`. It is NOT a target to reproduce by authoring or tuning a prompt.\n- **If that verification fails, STOP and report it** — do not reconstruct, paraphrase, or tune anything to force the hash. A mismatch means the copy or the hash function is wrong; that is a finding, not something to engineer around.\n\n### The v2 prompt is the authored deliverable — new hash, no reproduction target\n- v2 is authored (per the v2 content-match requirement above), hashed with the **same** hash function, and its **new** hash is recorded in every verdict's `judge_prompt_hash`.\n- There is **no** byte-reproduction target for v2. Do not attempt to hit any particular v2 hash.\n\n### The parsing core already exists — build on it, do NOT rebuild\nThis run is **seeded from the Stage 0 result of Launch 1** (voice-tutor commit `f34b470`, \"sprint 0 — core data contract + parsing defenses\", scored 97). The repo the run starts in ALREADY CONTAINS, and its hermetic tests already pass (39 green):\n- `coverage_judge.py` — claim-list loading, indexed-transcript loading/validation, markdown-fence stripping, strict verdict parsing, and the typed parse/validation error hierarchy.\n- `tests/test_coverage_judge.py` — the passing hermetic tests for the above.\n\nTherefore:\n- **Do NOT re-plan or rebuild Stage 0.** Verify the parsing core is present and its tests pass, then plan and build **Stage 1 onward only** (v2 prompt authored + hashed; single-invocation judge + verdict assembly; `union_coverage` + CLI; credentialed smoke with 17/17 label agreement), building ON the existing core rather than re-deriving it.\n- New Stage 1+ code and tests EXTEND the existing files (same `coverage_judge.py`, same `tests/test_coverage_judge.py`); do not duplicate the Stage 0 surface.\n\n### Wall clock\nPer-sprint wall clock raised to **45 minutes** for this launch (Stage 1 hit the 30-minute cap on a single generation attempt last time; the credentialed-smoke stage may legitimately need multiple iterations).\n\n\n---\n\n## Operator launch notes (added at launch time — NOT part of the frozen goal doc above)\n\nThese are environment/wiring facts the contract negotiation MUST respect. They do not\nchange the goal's acceptance bar above; they make it satisfiable inside a harness worktree.\n\n**Credentialed-smoke .env loading (CRITICAL).** The credentialed smoke needs\n`ANTHROPIC_API_KEY`. The app's `.env` is gitignored, so it does NOT exist inside the\nharness worktree. The smoke must obtain the key from the process environment, or read the\napp's `.env` by ABSOLUTE path `/Users/mattli/development/voice-tutor/.env` (equivalently\n`$HOME/development/voice-tutor/.env`). NEVER assume a worktree-relative `./.env` — there is\nnone. Never echo, log, or commit the key value.\n\n**Verifier command (hermetic layer).** The deterministic verifier runs:\n`uv run --with pytest pytest tests/test_coverage_judge.py -q`\nPut ALL hermetic (offline, model-call-mocked) tests in the single file\n`tests/test_coverage_judge.py` so this command exercises them. The hermetic suite must make\nNO network/LLM calls. The credentialed smoke is a SEPARATE, bounded step — run ONCE as\nacceptance evidence (~5 Haiku calls total, ~$0.07), NOT part of the per-iteration verifier\ncommand and NOT re-run per hermetic test.\n\n**Resolved absolute fixture source paths** (copy these into the module's test fixtures;\nUUIDs resolved at launch time):\n- Transcripts (PRIMARY union set — first four; last is the strictness-test session):\n  - f6148c26 → /Users/mattli/.voice-tutor/transcripts/matt/f6148c26-af09-491d-b644-1522db9f42c5.json\n  - 7beee170 → /Users/mattli/.voice-tutor/transcripts/matt/7beee170-47d1-418d-b35d-5cc56babaccc.json\n  - d33800bf → /Users/mattli/.voice-tutor/transcripts/matt/d33800bf-9322-4c97-9153-c9f356c42b24.json\n  - bb979045 → /Users/mattli/.voice-tutor/transcripts/matt/bb979045-72f5-4f84-8d8c-ae4bf137e711.json\n  - 12f3a30d (STRICTNESS TEST, judged vs the 71-claim shared doc) → /Users/mattli/.voice-tutor/transcripts/matt/12f3a30d-1324-4e94-bedf-e2d3fcb18d9f.json\n- Claim maps (`.claims.json` sidecars):\n  - 63-claim matt doc (doc_key matt/2aa66acc) → /Users/mattli/.voice-tutor/documents/matt/2aa66acc-1c4b-4d7d-83fe-b361fbe38523.claims.json\n  - 71-claim shared doc → /Users/mattli/.voice-tutor/documents/_shared/ac4b826f-b189-442e-98c0-a59bb066d600.claims.json\n- Eval set (frozen answer key + v1 prompt), in\n  `/Users/mattli/second-brain/products/voice-tutor/validation/coverage-experiment/`:\n  - labels.json (frozen answer key — READ ONLY, never modify) → .../labels.json\n  - judge-prompt-v1.md (repo-scheme hash `632b73a34b1a22b1`) → .../judge-prompt-v1.md\n\n**Live-code guardrail.** New files only. Do NOT modify `bot.py`, the server, existing\nprompts, `labels.json`, or any live path. `labels.json` is the frozen answer key.",
  "title": "coverage judge module",
  "startedAt": "2026-08-03T22:24:40.642Z",
  "runDir": "runs/voice-tutor-cj-base/2026-08-03-coverage-judge-module",
  "status": "running",
  "projectPath": "/Users/mattli/development/voice-tutor-cj-base",
  "sprints": [
    {
      "id": 0,
      "title": "v2 judge prompt authored + v1 provenance verified",
      "description": "Building ON the existing Stage 0 core in coverage_judge.py (do NOT rebuild parsing/loading). Add deterministic prompt-hash computation reused for both prompts. Copy judge-prompt-v1.md from the eval-set folder (/Users/mattli/second-brain/products/voice-tutor/validation/coverage-experiment/judge-prompt-v1.md) into module test fixtures byte-for-byte, and hash the copied bytes with the module's hash function asserting it equals 632b73a34b1a22b1 — this is provenance to VERIFY, not reproduce; if it fails, STOP and report, do not tune. Author the v2 prompt as a versioned in-module constant: v2 keeps v1's topic-adjacency strictness AND adds a content-match requirement (covered = tutor's explanation consistent with the claim's specific assertions — same substance, not merely same shape/topic/shared keywords; substituting different list members, mechanism, or contradicting specifics is NOT covered even if fluent/adjacent). Hash v2 with the SAME function; no reproduction target for v2. Hermetic tests (in tests/test_coverage_judge.py): v1 fixture hash == 632b73a34b1a22b1, v2 hash is deterministic and stable, hash function is pure. Verifier: uv run --with pytest pytest tests/test_coverage_judge.py -q stays green."
    },
    {
      "id": 1,
      "title": "single-invocation judge + verdict assembly",
      "description": "Add the judge invocation to coverage_judge.py: one model call per invocation (Haiku, temperature 0), passing the claim list + indexed transcript + v2 prompt. Assemble the output verdict: for every claim id exactly once {claim_id, covered: bool, turns: [int,...]}, plus metadata judged_at, model, judge_prompt_hash (= v2 hash). Wire in the transport-layer defenses building on the existing parsing core: markdown-fence stripping, truncation detection (parsed verdict count must equal claim count), missing/duplicate/unknown claim-id handling via the existing typed error hierarchy, and a bounded retry on malformed output. Hermetic tests in tests/test_coverage_judge.py with the model call MOCKED (no network): well-formed response parses to correct verdict set; fenced output stripped; truncated verdict list (count < claims) triggers detection then retry; malformed JSON triggers bounded retry then errors; verdict carries correct metadata and judge_prompt_hash. Keep suite green under the verifier command."
    },
    {
      "id": 2,
      "title": "union_coverage + standalone CLI",
      "description": "Add the pure function union_coverage(verdict_sets) -> {covered_ids, percentage}: merge verdicts across sessions by union of covered claim ids, percentage derived at read time per the design doc; no I/O, no model calls. Add the CLI: python -m coverage_judge --claims <file> --transcript <file> --out <file> (plus --cost-out JSON for cost accounting and --model/--prompt-version config), reading a claim list + indexed transcript, running one judge invocation, and writing the verdict object to --out. Do NOT wire ledger writes into any app path — the --cost-out JSON is sufficient. New files only; no changes to bot.py, server, prompts, or live paths. Hermetic tests in tests/test_coverage_judge.py (model call mocked): union merges overlapping/disjoint covered ids correctly and computes percentage; empty and single-session edge cases; CLI end-to-end wiring reads inputs, invokes the (mocked) judge, and writes valid --out and --cost-out JSON. Verifier suite stays green with zero network calls."
    },
    {
      "id": 3,
      "title": "credentialed smoke — 17/17 label agreement acceptance",
      "description": "Copy the frozen fixtures into module test fixtures from the resolved absolute paths: the four PRIMARY transcripts (f6148c26, 7beee170, d33800bf, bb979045) + the strictness session (12f3a30d) from /Users/mattli/.voice-tutor/transcripts/matt/, the 63-claim matt doc sidecar (/Users/mattli/.voice-tutor/documents/matt/2aa66acc-....claims.json) and the 71-claim shared doc sidecar (/Users/mattli/.voice-tutor/documents/_shared/ac4b826f-....claims.json), and labels.json (READ ONLY). Add a SEPARATE bounded credentialed-smoke script (NOT part of tests/test_coverage_judge.py, NOT run per hermetic test) that makes real Haiku calls — obtaining ANTHROPIC_API_KEY from the process env or the app .env at ABSOLUTE path /Users/mattli/development/voice-tutor/.env (never worktree-relative; never echo/log/commit the key). Acceptance bar (~5 Haiku calls, ~$0.07, run ONCE): the 16 upheld claims (c1,c2,c3,c5,c6,c8,c9,c15,c17,c21,c27,c28,c31,c46,c47,c48) covered in the union of the four primary sessions; c30 NOT covered (mandatory regression); strictness session 12f3a30d vs the 71-claim map yields ≤2 covered. If v2 flips any of the 16 upheld labels to not-covered, that is a FAILURE — iterate the v2 prompt (re-hashing, updating recorded hash) until all 17 labels agree, without trading recall for the c30 fix. If one specific label repeatedly disagrees on principled grounds, STOP and surface it in the run report rather than tuning to force agreement; never modify labels.json. Deliver the smoke script + a run report capturing the 17/17 result and cost."
    }
  ],
  "currentSprint": 0,
  "contractVersion": 0,
  "scores": [],
  "iterations": 0,
  "budgetSpentUsd": 0,
  "haltReason": null,
  "contractFreezeReason": null
}