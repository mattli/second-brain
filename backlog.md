> **Backlog convention:** items are 1–3 plain sentences, human-readable at a glance — what it is, why it matters, and a link to a detail doc if depth exists. Technical scoping, code paths, and specs go in linked docs (areas/ or the product folder), never inline here.

### dev-harness
> **Phase ② — "trustworthy on one project."** ✅ **Stop-early core SHIPPED 2026-07-15** (`main` @ `0356ce2`): the three stop-early items (from the csv2json trial 2026-07-07) — caps rework, honest CLI, verification-per-sprint, ≤6 sprint cap, `FINAL SCORE:` marker, "Paused not failed" summary — built subagent-driven, whole-branch reviewed on opus, and verified live (see Completed). Plan: [2026-07-14-phase-2-stop-early-implementation-plan](areas/dev-harness/planning/2026-07-14-phase-2-stop-early-implementation-plan.md). **Remaining under ②:** the read-only dashboard (below). **Watch-item:** the real-SDK usage-limit *error shape* is unconfirmed until an actual limit is hit.
- [ ] **Read-only live dashboard** (new, 2026-07-13) — stage 1 of the dashboard vision: a local web view of the active run (goal, current sprint/round/step, scores, elapsed time/spend), auto-refreshing from the run folder. Safe to build now (only renders existing run data); needs slightly finer-grained progress events for round-level detail. Config editing + a launch/stop button are **stage 2, deferred to phase ⑤** (wants real usage first). Design constraint: config has a single source of truth.
- [ ] **Transcript v2 — make run transcripts actually readable** (2026-07-16). Today's transcripts dump raw contracts and skip the story (no per-revision narrative — what was tried, what the evaluator docked, how scores moved). Fix is two parts: a renderer rewrite that works retroactively on old runs, then a small capture change so future runs keep the evaluator/verifier feedback they currently throw away. That second part (Layer 2) now has a live motivating case: run mrqghymn scored correct, verifier-passing work 3/0/4 and the trace recorded only the number, so the "why" had to be reconstructed by hand. Full spec + scoping: [2026-07-16-transcript-v2-scoping](areas/dev-harness/planning/2026-07-16-transcript-v2-scoping.md). Don't build yet.
- [ ] **Exclude SDK stall/backoff time from the sprint wall clock** (2026-07-17). Run mrpk71c5 stalled ~11 min on subscription-limit backoff at startup (free, pre-sprint) — but the same stall mid-sprint would burn the 30-min cap while doing no work, and a hung call can't be interrupted between checkpoints. Fix direction: pause the sprint clock during SDK waits, or route sustained backoff into the existing "paused, not failure" state. Don't build until a run actually gets capped by waiting.
- [ ] **Negotiation must not freeze unsatisfiable contracts** (2026-07-18) — *PAUSED: mechanism undecided, two attempts failed review.* Unwinnable-contract cause #3, after verifier-env-mismatch and evaluator-cwd: a self-run (mrqghymn) froze a contract pairing an exact file-allowlist ("only these 6 files") with "the verifier passes," which no artifact can satisfy — so the blind grader failed correct, verifier-passing code 3/0/4. **Attempt 1** (code detector that strips file-allowlist criteria) — failed review: pattern-matching free-text criteria both over- and under-matches; classifying a criterion is semantic, only the critic can do it. Archived on branch `archive/fix-satisfiable-contracts-2026-07-18`. **Attempt 2** (prompts: critic rejects allowlists + grader ignores file-set) — failed review on implementation, and surfaced the core limitation: **the critic's rejection is bypassable** — at the negotiation round-limit the harness freezes the contract even without agreement, so prompts alone can't *guarantee* satisfiability; a soundly-worded grader backstop is essential. On branch `fix/satisfiable-contracts-prompts` (unmerged). **Open decision:** accept prompts+grader (very-likely + safe-when-it-slips) vs. a structural fix (separate scope from acceptance criteria so the blind grader never sees scope — a hard, testable guarantee). Trigger: run mrqghymn. Full write-up on the branches' solutions doc (contract-file-allowlist-contradicts-tests.md).
- [ ] **Playwright verifier lane for UI goals** (2026-07-18). UI verification was part of the harness's original inspiration: add browser-driven checks (Playwright) as a verifier option alongside pytest, so contracts on UI goals can specify real browser assertions instead of fetch-level proxies. Trigger: first Voice Tutor UI run (recap browser — see products/voice-tutor/ideas.md) where fetch-level verification proves insufficient, or the live-transcript-stream goal, whichever comes first. Don't pre-build.
- [ ] **Goal-intake interview step** (2026-07-18). Instead of hand-writing harness goals from scratch, a pre-run step where the harness (or a goal-authoring skill) interviews Matt with clarifying questions — flow, verifiable criteria, design references, scope-as-intent — and emits a harness-ready goal; same spirit as compound-engineering/Superpowers brainstorm steps. Interim workflow: brainstorm the goal conversationally with Claude, paste the distilled result. Don't build until hand-written goals repeatedly produce gaps the negotiation critic can't fill.

### Agent Behavior
- [ ] Periodic README audit — on a regular schedule, scan all folders in the vault for README files, compare contents against the current folder contents, and flag or update anything stale. Could be a NanoClaw cron task or a Claude Code session trigger. Define "stale" heuristic: file listed in README that no longer exists, or new files not mentioned.

### Skills & Tools
- [x] **Configure cheaper models for mechanical review subagents** — resolved 2026-07-19. Captured actual per-subagent models from existing session logs (no fresh run needed): the workflow-backed `/code-review` generates an ad-hoc `Workflow` script that passes **no `model:` on any `agent()` call**, so every subagent inherits the session model — measured **16/16 on Opus** (`wf_009a443e`, 7/18). So the Sonnet tiering the ce-code-review skill *designs* is **not** applied on the workflow path; verifiers were running Opus, not Sonnet. Fix (option 1): added an enforcement note to dotfiles `CLAUDE.shared.md` — pass `model:'sonnet'` on verifier/sweep + non-judgment finder waves, keep judgment finders (`correctness`/`security`/`adversarial`) + synthesis on the session model. Durable proof still pending: the next workflow-backed review should show verifiers on Sonnet.
- [ ] Install frontend-design skill in Claude Code — enables polished UI component generation as part of the Claude Design → v0 → Claude Code workflow. See products/strategy/design-workflow.md.
- [ ] Install add-compact — adds /compact command for context compaction; needed when Phase 2 PM agent is built
- [ ] Install add-telegram-swarm — agent teams in Telegram, each subagent gets its own bot identity; relevant to Phase 3. Potential use case: adversarial agent debates — one optimistic, one pessimistic, one focused on execution risk — for stress-testing product decisions like pmtxt direction or YC application strategy. Related to the debate-perspectives project in the vault.
- [ ] Investigate add-ollama-tool — local models as tools inside containers, zero API cost for cheap tasks

### Intelligence Pipeline
- [ ] Make weekly summary shorter — current output is too long; investigate prompt changes or structural cuts to reduce length without losing signal.
- [ ] Schedule atomic notes compiler — weekly NanoClaw task that compiles atomic notes from the previous 7 days of Telegram conversation history (main group). Output to resources/atomic-notes/. Pipeline scaffold already exists at projects/atomic-notes/; one-off experiment ran 2026-04-13 and produced 13 usable notes. Decide cadence, prompt format, and whether notes should update in place or append new. Evaluate after ~4 runs whether output is actually useful or just noise.
- [ ] Investigate whether it would be worthwhile to internal link weekly/monthly summaries back to weekly/daily briefings.
- [ ] ~Aug 1: judge briefing test against products/briefing/validation-gate.md

### Cold Mountain (coldmountain.ai)


### dotmd
- [ ] Fix hardcoded `--version` output — `src/cli/index.ts:15` calls `.version("0.1.0")` literally, so `dotmd --version` reports `0.1.0` regardless of the installed package version. Replace with a `package.json` read (ESM pattern: `JSON.parse(readFileSync(join(dirname(fileURLToPath(import.meta.url)), "../../package.json"), "utf-8")).version`) so it stays in sync automatically.
- [ ] Revisit extensibility for `~/.app-data/` runtime dirs — currently each new app that stores AI files outside the project repo (e.g. voice-tutor's `~/.voice-tutor/`) has to be added to `scan_roots` manually. Once this happens a few more times, build a repo-side opt-in (`.dotmd.yaml` declaring `extra_roots`) so projects can self-declare their runtime dirs. Don't pre-build — wait for the third manual addition before investing.

### Voice Tutor

### NanoClaw
- [ ] Integrate Google Calendar with NanoClaw — connect calendar so NanoClaw can read events, add reminders, and surface scheduling context. Relevant to to-do list workflow and scheduled task reminders.
- [ ] Diagnose Telegram response latency — messages to the main group take 10–20+ seconds to get a response. Unknown whether it's container cold start, context loading, MCP startup, model latency, or tool calls within the response. Run diagnostic: send a trivial message ("hi") and a vault-lookup message back to back, compare times. If both slow → cold start or model latency. If only complex one slow → context/tool overhead. Possible knobs: warm containers, lower MAX_MESSAGES_PER_PROMPT, trim CLAUDE.md, route simple tasks to Haiku. Note (2026-04-30): streaming progress (`NANOCLAW_STREAMING_PROGRESS=1`) now masks the wait conversationally — first 🔧 tool note appears within ~1s — but root cause is still unsolved.
- [ ] Update Phase 2 in the NanoClaw README — current vision (PM Best Practices Layer) no longer reflects actual plans; revisit when clearer on what, if anything, replaces it.
- [ ] Configure /remote-control — NanoClaw feature already merged, not yet configured. Starts a Claude Code session on the Mac Mini directly from Telegram, returns a browser URL. Full host access without SSH. Priority: next time at Mac Mini.
- [ ] Scheduled task to rotate `session-tasks.md` — the file is read every session and grows unbounded (hit ~260 KB before a manual trim 2026-07-17). Have a NanoClaw cron periodically archive entries older than 2 weeks into `_archive/session-tasks/`, split by month, leaving only recent entries + the archive-pointer header. Preferred over a manual end-of-session step so it's hands-off.

### Infrastructure
- [ ] Research terminal apps with Claude Code notifications — find a terminal (or terminal + integration) that sends a notification or badge when Claude Code finishes a task or needs a permission grant. Candidates: iTerm2 triggers, Warp AI-native terminal, Ghostty, or a shell hook on the Claude Code process. Goal: stop checking back manually mid-task.
- [ ] Fix macOS login keychain — BOTH machines. **MacBook Pro:** Claude Code can't write to login keychain ("User interaction is not allowed" error). **Mac Mini:** every `git push` emits `failed to get/store: -25308` (`errSecInteractionNotAllowed`) — the `osxkeychain` git credential helper can't read/cache the credential (pushes still complete, but the credential is never stored, so it nags on each push; observed repeatedly during the 2026-07-16 voice-tutor harvest pushes). Same root cause on both: the login keychain is locked or its password is out of sync with the account password. Fix per machine: try `security unlock-keychain ~/Library/Keychains/login.keychain-db` first; if that fails, reset via Keychain Access → login keychain → Edit → Change Password. Not blocking (pushes/credential reads still work interactively), but will keep biting until repaired.
- [ ] Mobile Claude vault access — mobile Claude has no read/write access to the vault; planning sessions can't capture ideas directly. Options: (1) GitHub MCP connector — vault already on GitHub, lowest effort, works today, writes go through git commits; (2) Hosted MCP server via Cloudflare tunnel — more powerful, more work, security surface; (3) Telegram bridge — forward intent from mobile Claude to existing Second Brain agent, indirect but reuses infra.
- [ ] Investigate how to reliably inject current date/time into LLM context. This was done on Claude desktop. Observing how often I run into the issue in claude code & telegram.
- [ ] Sync `settings.json` itself through the dotfiles repo (stretch) — so the sync hooks and other Claude Code config don't themselves drift across machines. Apply the same shared/local split if any settings turn out to be machine-specific. Do only after the sync hooks prove out; don't over-build ahead of need.

### Completed
- [x] Debug broken links in the wiki — identify and fix broken wikilinks or dead references across wiki pages.
- [x] Sat 7/18: briefing edition 2 — posted to X 2026-07-18 as "This Week in Indie Product Threads" (raw-list format, typographic cover). Format change applied to instructions (weekly-products.md v2.0, monthly-summary.md v2.6): output is now a post-ready raw list, engagement data banned from output.
- [x] ~~Harvest BOTH run branches' Voice Tutor work into the real repo~~ — **done 2026-07-16, shipped to `feat/study-companion-mode` (`e0300af`, pushed). See Completed.**
- [x] Fix Claude Code jitter in tmux — streaming output flickers over Ghostty→SSH→tmux; two known fixes (upgrade tmux to ≥3.4, then `/tui fullscreen` in Claude Code). ~5 min. Walkthrough: [claude-code-tmux-jitter-fix](resources/claude-code-tmux-jitter-fix.md).
- [x] Make run outputs self-locating — done 2026-07-18 (dev-harness `main` @ `f57a595`, pushed). Built by running dev-harness **on itself** (run mrqghymn), independently reviewed, review findings fixed, 114 tests green. Branches are now `run-<runId>` (dropped the unreadable goal-slug); the transcript header self-locates with absolute paths — `Code: branch run-<id> in <abs project path>` and `Records: <abs run dir>` (threaded `projectPath?` through `RunState`). Review caught a stale `run/*` glob in the e2e smoke test (would silently miss the flat branch) + a dead `goal` param — both fixed. **The self-run also exposed unwinnable-contract cause #3** (it graded its own correct work 3/0/4 — see the active "Negotiation must not freeze unsatisfiable contracts" item); the invalid grade was overruled with cause and the feature merged on the strength of the real verifier.
- [x] Harvest the harness week's Voice Tutor work into the real repo — done 2026-07-16, shipped to `feat/study-companion-mode` (`e0300af`, pushed). Brought sprint-0's `documents.py` characterization tests + the `bot.py`→`session_state.py` relocation (`session_state.py` + the 6 behavior-pinning test files) into `~/Development/voice-tutor`; **excluded** the 3 HEAD-comparing tests (manifest/golden/botmove) per the caveat; skipped the clone's `.harness-venv`/worktree gitignore cruft. **The push exposed an 11-commit-stale Mini checkout** (origin/feat had Matt's own older work — `/study/` redesign, `/client/`→`/chat/` rename, per-doc summaries, TTS flag — pushed elsewhere, never pulled here); root cause was reading "up-to-date" from stale refs without fetching → new **"Fetch Before You Trust Repo State"** rule in `~/.claude/CLAUDE.md`. Rebased onto the true tip; verbatim-move **re-verified byte-identical** against the NEW `bot.py` (rebase was conflict-free for exactly that reason). The remote's per-doc-summaries redesign made `list_documents()` async + added a `summary` key, so the documents characterization tests **correctly failed and were re-pinned** as their own approved commit (`asyncio.run` + updated key-sets; `_generate_summary` stubbed via a module-scoped autouse fixture for hermeticity). Final suite 70 passed / 0 skipped / offline; Matt smoke-tested `/study/` + `/chat/` before the push. `harvest/harness-week1` kept as a local backup. Context: products/voice-tutor/planning/harness-characterization-run-draft.md.
- [x] Voice Tutor characterization run — executed 2026-07-16; fixed a harness bug found mid-run. Both sprints ran in the clone as separate single-sprint runs: `documents.py` tests (b79d8ron5, 96, `26 passed`) and the `bot.py`→`session_state.py` relocation (`mrn0iav3`, 96/100, relocation verified byte-for-byte verbatim). **The relocation launch surfaced the second distinct cause of an unwinnable contract:** the NEGOTIATE critic ran with an undefined `cwd` and inspected dev-harness's own repo, so it "saw" no `bot.py` and froze an `UNSATISFIABLE-AS-SCOPED` no-op contract. Fixed (sighted critic → worktree cwd; blind scorer → no cwd, encoded in signatures) after an independent workflow review caught that my first patch wrongly gave the *scorer* a cwd too (blind-boundary leak + false-pass). Shipped to dev-harness `main` @ `c3cc9d1` (pushed, 98 tests green). Verdicts: prior EVALUATE scores need no asterisk; b79d8ron5's contracts need no re-read. Learnings: `docs/solutions/conventions/evaluator-cwd-blind-scorer-sighted-critic.md` + `.../harness-generated-tests-keyed-on-git-head.md` (+ `CONCEPTS.md`, CLAUDE.md sticky notes). **Remaining:** harvest both branches into the real repo (still-open Voice Tutor item, with the HEAD-keyed-test caveat).
- [x] dev-harness Phase ② stop-early core — shipped 2026-07-15 (`main` @ `0356ce2`, pushed). The three csv2json-trial stop-early items built as one coherent design: caps rebuilt around a subscription (wall-clock primary/per-sprint, dollar off-by-default, usage-limit graceful pause, "paused not failure", approximate-cap honesty), ≤6 sprint cap in code, honest CLI flags (`--wall-clock-ms`/`--max-iterations` wired), verification moved into every sprint (planner + contract prompts), and the `parseScore` residual closed via a unique `FINAL SCORE:` marker. Built subagent-driven (fresh implementer + independent reviewer per task, 9 task groups); final whole-branch review on opus caught + fixed one usage-limit-detector over-match. 96 unit + 2 gated E2E pass. **Verified live** ($0.42 run scored 98/100; the sprint's frozen contract required tests and the generator produced `sum.js` + `sum.test.js` — confirming verification-per-sprint against the real model). Plan doc: `areas/dev-harness/planning/2026-07-14-phase-2-stop-early-implementation-plan.md`. Deferred Minors + the usage-limit real-shape watch-item recorded in the repo SDD ledger.
- [x] Set up a dotfiles repo — done 2026-07-14. Private repo `github.com/mattli/dotfiles` syncs shared Claude config across the Mac Mini + MacBook Pro via a shared/machine-local split: shared rules in `claude/CLAUDE.shared.md` (synced), machine-specific bits in each machine's local `~/.claude/CLAUDE.md` (which `@import`s the shared file). Auto-syncs via SessionStart/SessionEnd hooks + a `dotsync` manual command (see next item). **Decided NOT to migrate shell aliases** into the repo — keeping each machine's `~/.zshrc` separate on purpose (aliases rarely change, so sync payoff is low; update each by hand as needed). Revisit only if copying aliases between machines becomes annoying. (Remaining optional stretch: sync `settings.json` itself — still listed under Infrastructure.) Setup + templates documented in the repo README.
- [x] Automate dotfiles sync via Claude Code hooks — done 2026-07-14, **both machines**. **SessionStart hook** (`git -C ~/dotfiles pull --autostash`) + **SessionEnd hook** (commit + push any changes) in each machine's `~/.claude/settings.json`; **`dotsync`** shell function in each `~/.zshrc` as a manual terminal force-sync. Templates (`claude/hooks.json`, `zsh/dotsync.zsh`) live in the repo for paste-free setup. Dropped the `/dotsync` slash-command idea: the real limit is that Claude Code loads CLAUDE.md once per session with **no mid-session hot-reload** (confirmed via docs), so a persistent session needs `pull` + `/compact` by hand — the hooks cover the common start-fresh-sessions workflow, which is enough. Syncing `settings.json` itself remains a separate stretch item.
- [x] Investigate `ce-compound` skill — resolved 2026-07-13 (product-planning session). **What it does:** writes a worked-example learning to `docs/solutions/` and, in repos set up for it (dev-harness), drops the short "sticky note" rule + pointer into the project CLAUDE.md. **Adopted as:** a step in the global End-of-Session Ritual (`~/.claude/CLAUDE.md` step 4) — run `ce-compound` when a session solved+verified a non-trivial reusable problem, and ensure the lesson has a sticky note in the *project* CLAUDE.md (rejected the fragile standalone "auto-invoke" framing — a CLAUDE.md rule is a nudge, not real automation). **Which repos qualify:** only those that keep a `docs/solutions/` store. Key insight: `docs/solutions/` files are not read at session start (only CLAUDE.md is), so every solutions doc needs a CLAUDE.md pointer or it's an orphan.
- [x] Persist frozen contract contents to trace + run dir — done 2026-07-08 (dev-harness `main`, merge fb1bbc3). Embedded the frozen contract in the NEGOTIATE trace event (`TraceEvent.contract`) and rendered its criteria in the transcript, so a run shows what each score was measured against. Dropped the separate `contract-v<N>.json` file (the backlog's "and/or") on YAGNI grounds — the trace embed is self-contained. Independently reviewed at high effort; a renderer newline-sanitize + missing-array guard fix was applied.
- [x] Record contract freeze reason in trace + state — done 2026-07-08 (dev-harness `main`, merge fb1bbc3). `negotiate()` now returns `{contract, freezeReason}` (`agreement` | `round-cap`); the reason is snapshotted in `RunState.contractFreezeReason` and the NEGOTIATE transcript line reads `frozen (agreement|round-cap)` so a cap-forced freeze (adversarial gate bypassed) is visible. Folded into the same pass as the frozen-contract item since both touch negotiate.ts + run.ts.
- [x] Verify the C1/C2 joint success condition actually passed — verified 2026-07-08 against committed evidence from the 2026-07-07 post-fix run. `runs/_archive/e2e/transcript.md` shows the on-goal `sum.js` loop passing (scores [100, 98], status=passed). C1/C2 fix commit `2bdbf27` and verification doc commit `ab6aa25` both landed. `docs/verification-run-2026-07-07.md` records `RUN_E2E=1 npx vitest run` → 46 tests all passed (0 skipped), C2 discrimination probe on-goal sum.js=100 vs off-goal isPalindrome.js=0 (gap 100, floor >85/<85/gap≥20). Verdict: C1/C2 guarantee VERIFIED. (No design note with `awaiting-sign-off` status found — superseded by the verification-run doc.)
- [x] Investigate silent wiki task failure — 3am run failed (root cause: WiFi DNS failure, not API issue) with no Telegram notification. Container exited non-zero but NanoClaw treated it as complete. Fix: catch non-zero container exits and notify. Separate from the network issue — hardwiring to ethernet should prevent the DNS failures, but the missing notification is still a bug.
- [x] Hardwire Mac Mini to ethernet — WiFi DNS failures at 3am killed the readwise-wiki scheduled task on 2026-04-13. Prevents future overnight network flakiness.
- [x] Install update-nanoclaw — pull upstream changes with preview and selective cherry-pick; run roughly once a month (2026-04-12)
- [x] Upstream NanoClaw release monitoring — subscribed to the NanoClaw GitHub repo with Watch → Custom → Releases only. Email arrives on every upstream release; glance at notes, act if "BREAKING" or "migration" appears. Rule: 1 breaking change behind = update within ~2 weeks; 2 stacked = update now. (2026-04-13)
- [x] Update NanoClaw v1.2.15 → v1.2.52 — merged 365 upstream commits, kept native credential proxy (Parallel AI, GitHub token, Readwise MCP routes), preserved Telegram multi-bot, per-group model config. OneCLI migration deferred. (2026-04-13)
- [x] Add session start instructions to main group CLAUDE.md (both machines)
- [x] **Readwise wiki compiler** — Bootstrapped wiki with 12 pages from 65 saves, scheduled as weekly NanoClaw task (Fridays 10pm). Instructions at `wiki/instructions/readwise-wiki.md`, output at `resources/wiki/`. (2026-04-05)
- [x] Set up tmux on Mac Mini — keeps SSH sessions alive if connection drops; useful for long Claude Code sessions
- [x] Readwise MCP integration — connected to NanoClaw main group via mcp-remote stdio bridge + OAuth (2026-03-29)
- [x] Investigate Parallel AI MCP skill — installed Search + Task MCP via credential proxy; confirmed working (2026-03-23)
- [x] Add start instruction to check README to global claude.md files (both machines) (2026-03-25)
- [x] Add end instruction to update README "Check if any README.md files in projects you touched need updating to reflect what was done this session" (2026-03-25)
- [x] Automated memory curation — Practical starting point is a curated memory.md read at session start
- [x] Update main Telegram group CLAUDE.md — add communication style preferences (no value judgments on content)
- [x] Investigate last30days skill — better briefing source coverage for Reddit, X, HN (Phase 2)
- [x] Explore using last30days for a new product briefing — installed IPC bridge, X-only research working (2026-03-26)
- [x] Disable iCloud on Mac Mini — no longer needed, may cause conflicts
- [x] Include detected keyword in product briefing export, link, signal? — source attribution added to each product entry (2026-03-27)
- [x] Tune product briefing research topics — switched from keyword search to thread reply scraping (2026-03-27)
- [x] Bird search product aggregation — built full pipeline: thread search → product extraction → URL resolution → categorization; consolidated to weekly schedule (2026-03-28)
- [x] Update coldmountain.ai — added dotmd card, Readwise wiki mention, merged Intelligence/Research and Specification/Execution sections, updated tagline and tech stack (2026-04-13)
- [x] Wire up Vercel deploy hook so coldmountain.ai rebuilds on weekly wiki compile — added `POST /cold-mountain-deploy` proxy route reading `COLD_MOUNTAIN_DEPLOY_HOOK` from `.env`; curl trailer in readwise-wiki.md + folder-review.md fires only when commit produces changes; also fixed latent `/workspace/extra/second-brain` → `/workspace/extra/vault` path bug in folder-review.md (2026-04-21)
- [x] ~~Add source count to wiki front matter~~ — cancelled 2026-06-22. Wiki pipeline collapsed to 2 stages; the weekly wrap-up that would have read the field is archived. The strict frontmatter rule stays; this item is obsolete.
- [x] ~~Track every Readwise save's wiki disposition~~ — cancelled 2026-06-22. Original premise was Tier C/D tracking, but the new pipeline has only two paths (dropped by pre-filter, or dispatched to per-doc-worker). The list-maker log already records every dispatch and every drop; git history on the Mac Mini is the durable record. The reverse-lookup ("which wiki pages cite this Readwise source") is answerable via `grep` against the wiki. No separate manifest needed.
- [x] ~~Reader-facing weekly wiki digest~~ — cancelled 2026-06-22. Obviated by pipeline simplification: the weekly wrap-up that produced `last-run-manifest.md` is archived, and the "what's new in this article" need is now solved at the per-page level by the `## Recent Updates` log maintained by per-doc-workers.
- [x] ~~Readwise wiki → weekly summary integration~~ — cancelled 2026-06-22. The weekly wrap-up is archived; this evaluation can't happen as scoped. If wiki/weekly-summary cross-pollination becomes valuable again, redesign around the simpler two-stage pipeline rather than reviving the wrap-up.
